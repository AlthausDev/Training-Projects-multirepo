<Page x:Class="VITP._12_Ommnisiah.Pages.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VITP._12_Ommnisiah.ViewModel"       
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Page.DataContext>
        <local:BeerListViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <!-- Estilo para las celdas del DataGrid -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- Estilo para los encabezados del DataGrid -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!-- Estilo para las filas del DataGrid -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Page.Resources>


    
    <Grid>
        <DataGrid ItemsSource="{Binding Beers}" AutoGenerateColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" />
                <DataGridTextColumn Header="ABV" Binding="{Binding Abv}" Width="*" />
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Estilo" Binding="{Binding Style}" Width="*" />
                <DataGridTextColumn Header="Cervecera" Binding="{Binding Brewer}" Width="*" />
                <DataGridTextColumn Header="Ciudad" Binding="{Binding City}" Width="*" />
                <DataGridTextColumn Header="Estado" Binding="{Binding State}" Width="*" />
                <DataGridTextColumn Header="Etiqueta" Binding="{Binding Label}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
