package com.althaus.dev.cinemaNexus.ui.theme

import androidx.compose.ui.graphics.Color

// =======================================
// Tema Claro
// =======================================

// -------------------------
// Colores de Fondo
// -------------------------

/** Fondo principal en el tema claro. */
val LightBackgroundMain = Color(0xFFFFFFFF)

/** Fondo de superficies elevadas. */
val LightBackgroundElevated = Color(0xFFF5F5F5)

/** Fondo para mensajes de error. */
val LightBackgroundError = Color(0xFFFFEBEE)

// -------------------------
// Colores Principales
// -------------------------

/** Color principal en el tema claro. */
val LightPrimary = Color(0xFF6200EE)

/** Variante del color principal. */
val LightPrimaryVariant = Color(0xFF3700B3)

/** Texto sobre el color principal. */
val LightOnPrimary = Color(0xFFFFFFFF)

// -------------------------
// Colores Secundarios
// -------------------------

/** Color secundario en el tema claro. */
val LightSecondary = Color(0xFF03DAC6)

/** Variante del color secundario. */
val LightSecondaryVariant = Color(0xFF018786)

/** Texto sobre el color secundario. */
val LightOnSecondary = Color(0xFF000000)

// -------------------------
// Colores de Texto
// -------------------------

/** Texto principal sobre el fondo claro. */
val LightTextPrimary = Color(0xFF000000)

/** Texto secundario o deshabilitado en el fondo claro. */
val LightTextSecondary = Color(0xFF757575)

// -------------------------
// Colores de Error
// -------------------------

/** Color de error principal en el tema claro. */
val LightError = Color(0xFFB00020)

/** Variante del color de error en el tema claro. */
val LightErrorVariant = Color(0xFFCF6679)

/** Texto sobre el color de error en el tema claro. */
val LightOnError = Color(0xFFFFFFFF)


// =======================================
// Tema Oscuro
// =======================================

// -------------------------
// Colores de Fondo
// -------------------------

/** Fondo principal en el tema oscuro (negro profundo). */
val DarkBackgroundMain = Color(0xFF121212)

/** Fondo intermedio para degradados (gris oscuro). */
val DarkBackgroundMid = Color(0xFF1A1A1A)

/** Fondo de superficies elevadas (gris más claro). */
val DarkBackgroundElevated = Color(0xFF242424)


/** Fondo para mensajes de error. */
val DarkBackgroundError = Color(0xFF3A1D1F)

// -------------------------
// Colores Principales
// -------------------------

/** Color principal en el tema oscuro. */
val DarkPrimary = Color(0xFFFFFFFF)

/** Variante del color principal. */
val DarkPrimaryVariant = Color(0xFF3700B3)

/** Texto sobre el color principal. */
val DarkOnPrimary = Color(0xFF000000)

// -------------------------
// Colores Secundarios
// -------------------------

/** Color secundario en el tema oscuro. */
val DarkSecondary = Color(0xFF849595)

/** Variante del color secundario. */
val DarkSecondaryVariant = Color(0xFF849595)

/** Texto sobre el color secundario. */
val DarkOnSecondary = Color(0xFF000000)

// -------------------------
// Colores de Texto
// -------------------------

/** Texto principal sobre el fondo oscuro. */
val DarkTextPrimary = Color(0xFFFFFFFF)

/** Texto secundario o deshabilitado en el fondo oscuro. */
val DarkTextSecondary = Color(0xFFE0E0E0)

// -------------------------
// Colores de Error
// -------------------------

/** Color de error principal en el tema oscuro. */
val DarkError = Color(0xFFCF6679)

/** Variante del color de error en el tema oscuro. */
val DarkErrorVariant = Color(0xFFB00020)

/** Texto sobre el color de error en el tema oscuro. */
val DarkOnError = Color(0xFFFFFFFF)
